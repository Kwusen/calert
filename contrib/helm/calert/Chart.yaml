apiVersion: v2
name: calert
# Maintainer-owned chart version. Publish workflows use this as the floor and the
# default for deliberate chart releases; automated refreshes patch-bump above the
# highest published version. Bump this (minor/major) for a deliberate chart overhaul.
version: v2.3.0
description: A Helm chart for the calert which uses Alertmanager webhook receiver to receive alerts payload, and pushes this data to Google Chat webhook endpoint.
type: application
keywords:
  - monitoring
  - alerting
  - prometheus
  - alertmanager
  - alertmanager-webhook
  - google-chat
home: https://github.com/mr-karan/calert
sources:
  - https://github.com/mr-karan/calert
maintainers:
  - name: Karan Sharma
    email: hello@mrkaran.dev
    url: https://github.com/mr-karan
appVersion: v2.3.0
